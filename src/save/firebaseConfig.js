// Fill this in with your own Firebase project's config to enable cloud
// saves (Firebase Console -> Project settings -> General -> Your apps ->
// SDK setup and configuration -> Config). Firestore must be enabled on
// the project, and Anonymous sign-in must be enabled under
// Authentication -> Sign-in method.
//
// Leaving apiKey/projectId empty (the default) keeps BACKDOOR running on
// LocalStorage-only saves - nothing else needs to change, cloud sync is
// purely additive and never required.
export const firebaseConfig = {
  apiKey: '',
  authDomain: '',
  projectId: '',
  storageBucket: '',
  messagingSenderId: '',
  appId: ''
};
